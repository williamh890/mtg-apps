import { CardSelectorComponent } from './card-selector.component';

describe('CardSelectorComponent', () => {
  let component: CardSelectorComponent;

  it('should create', () => {
    component = new CardSelectorComponent();
    expect(component).toBeTruthy();
  });
});
